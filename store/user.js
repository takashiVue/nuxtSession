
export const state = () => ({
    username: 'hukuhoge'
  })
  
  export const getters = {
      username: state=>state.username
  }
  
  export const mutations = {
    serUsername (state, user) {
      state.username = username
    }
  }
  
  export const actions = {
    
  
  }
  